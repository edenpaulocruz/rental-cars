<h1>Categoria de carros</h1>

<table>
  <thead>
    <tr>
      <th>Categoria</th>
      <th>Diária</th>
    </tr>
  </thead>
  <tbody>
    <% @car_categories.each do |category| %>
    <tr>
      <td><%= link_to category.name, category %></td>
      <td><%= category.daily_rate %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<% if @car_categories.empty? %>
  <p>Nenhuma categoria de carros cadastrada</p>
<% end %>

<%= link_to 'Voltar', root_path %>